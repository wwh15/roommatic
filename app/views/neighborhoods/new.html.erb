<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New neighborhood</h1>

  <%= render "form", neighborhood: @neighborhood %>

  <%= link_to "Back to City", city_path(@city), class: "button-back" %>


</div>
